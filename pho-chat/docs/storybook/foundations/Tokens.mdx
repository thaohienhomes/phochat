import { Meta } from '@storybook/blocks';

<Meta title="Foundations/Tokens" />

# Tokens (Light/Dark)

Live preview of key color tokens and radius. These map 1:1 to CSS variables used in the app.

## Colors (Light Theme)

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(5, minmax(120px, 1fr))', gap: '12px' }}>
  {[
    ['background','var(--background)'],
    ['foreground','var(--foreground)'],
    ['primary','var(--primary)'],
    ['primary-foreground','var(--primary-foreground)'],
    ['secondary','var(--secondary)'],
    ['secondary-foreground','var(--secondary-foreground)'],
    ['muted','var(--muted)'],
    ['muted-foreground','var(--muted-foreground)'],
    ['accent','var(--accent)'],
    ['accent-foreground','var(--accent-foreground)'],
    ['destructive','var(--destructive)'],
    ['border','var(--border)'],
    ['input','var(--input)'],
    ['ring','var(--ring)'],
  ].map(([name, css]) => (
    <div key={name} style={{ border: '1px solid var(--border)', borderRadius: '8px', overflow: 'hidden' }}>
      <div style={{ height: 48, background: css }} />
      <div style={{ padding: 8, fontSize: 12 }}>{name}</div>
    </div>
  ))}
</div>

## Charts

<div style={{ display: 'flex', gap: 12, flexWrap: 'wrap' }}>
  {['--chart-1','--chart-2','--chart-3','--chart-4','--chart-5'].map((n) => (
    <div key={n} style={{ width: 96, border: '1px solid var(--border)', borderRadius: 8 }}>
      <div style={{ height: 48, background: `var(${n})` }} />
      <div style={{ padding: 8, fontSize: 12 }}>{n}</div>
    </div>
  ))}
</div>

## Sidebar

<div style={{ display: 'flex', gap: 12, flexWrap: 'wrap' }}>
  {['--sidebar','--sidebar-foreground','--sidebar-primary','--sidebar-primary-foreground','--sidebar-accent','--sidebar-accent-foreground','--sidebar-border','--sidebar-ring'].map((n) => (
    <div key={n} style={{ width: 140, border: '1px solid var(--border)', borderRadius: 8 }}>
      <div style={{ height: 48, background: `var(${n})` }} />
      <div style={{ padding: 8, fontSize: 12 }}>{n}</div>
    </div>
  ))}
</div>

## Radius

<div style={{ display: 'flex', gap: 16, alignItems: 'center' }}>
  {[
    ['--radius-sm','var(--radius-sm)'],
    ['--radius-md','var(--radius-md)'],
    ['--radius-lg','var(--radius-lg)'],
    ['--radius-xl','var(--radius-xl)'],
  ].map(([label]) => (
    <div key={label} style={{ width: 100, height: 60, background: 'var(--muted)', border: '1px solid var(--border)', borderRadius: '12px', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 12 }}>
      {label}
    </div>
  ))}
</div>

